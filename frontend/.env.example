# API URL for production (optional). Leave unset for local dev (http://localhost:8000).
# Set this in Vercel: Project → Settings → Environment Variables
# VITE_API_URL=https://your-backend-url.up.railway.app
